['ant', 'bear', 'caterpillar'].pop.size

# pop() Removes and returns trailing arguments.
# size() Returns the count of characters (not bytes) in self:
# The statement will return the size of 'caterpillar'
# => 11
