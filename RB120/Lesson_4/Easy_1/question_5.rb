class Fruit
  def initialize(name)
    name = name
  end
end

class Pizza
  def initialize(name)
    @name = name
  end
end

# Instance variables are initialized with a `@` as a prefix. `@name` from the
# `Pizza` class is the only instance variable.
